package kerberos.spring.management.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class SwaggerStubController {
  /*  
    @GetMapping("/null/swagger-resources") public ResponseEntity<String> swagger() throws InterruptedException {Thread.sleep(20000);return ResponseEntity.ok("{}");}
    @GetMapping("/null/swagger-resources/configuration/security") public ResponseEntity<String> swaggerResourceSecrity() throws InterruptedException {Thread.sleep(20000);return ResponseEntity.ok("{}");}
    @GetMapping("/null/swagger-resources/configuration/ui") public ResponseEntity<String> swaggerResourceUi() throws InterruptedException {Thread.sleep(20000);return ResponseEntity.ok("{}");}
    */
}
